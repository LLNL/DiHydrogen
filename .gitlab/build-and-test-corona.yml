################################################################################
## Copyright 2019-2023 Lawrence Livermore National Security, LLC and other
## DiHydrogen Project Developers. See the top-level LICENSE file for details.
##
## SPDX-License-Identifier: Apache-2.0
################################################################################

default:
  id_tokens:
     SITE_ID_TOKEN:
        aud: https://lc.llnl.gov/gitlab

stages:
  - build

include:
  local: "/.gitlab/build-and-test-common.yml"

# Note: We load gcc/10.3.1-magic to get the right MPI but it's not
# (directly) used.
#
# Note: There are issues with /usr/tce/bin/python3 (the default) and
# build systems (in particular, it's not clear that all the
# includes/libs are setup properly under the "/usr/tce" prefix). So I
# load a module to get a real prefix.
rocm-6-4-2-corona:
  variables:
    COMPILER_FAMILY: amdclang
    MODULES: "gcc/13.3.1-magic mvapich2/2.3.7 rocm/6.4.2 cmake/3.30.5 python"
  extends: [.build-and-test-on-corona, .build-and-test]

gcc-13-3-1-cpu-corona:
  variables:
    COMPILER_FAMILY: gnu
    MODULES: "gcc/13.3.1-magic mvapich2/2.3.7 cmake/3.30.5 python"
  extends: [.build-and-test-on-corona, .build-and-test]

clang-19-1-3-cpu-corona:
  variables:
    COMPILER_FAMILY: clang
    MODULES: "clang/19.1.3-magic mvapich2/2.3.7 cmake/3.30.5 python"
  extends: [.build-and-test-on-corona, .build-and-test]

# FIXME (trb 12/15/2025): The coverage tools are failing, and it's not
# clear why. While I want to resolve this, of course, this is not the
# primary task at hand right now.

# gcc-13-3-1-coverage-cpu-corona:
#   variables:
#     COMPILER_FAMILY: gnu
#     MODULES: "gcc/13.3.1-magic mvapich2/2.3.7 cmake/3.30.5 python"
#     WITH_COVERAGE: "1"
#   extends: [.build-and-test-on-corona, .build-and-test-coverage]

.build-and-test-on-corona:
  stage: build
  tags: [corona, batch]
